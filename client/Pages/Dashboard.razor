@page "/"
@using client.Services
@inject StudySessionService SessionService
@inject UserService UserService
@inject AuthenticationStateProvider AuthStateProvider

<AuthorizeView>
    <Authorized>
        <h3>📊 Dashboard</h3>

        <p>Welcome, @userName!</p>

        <div class="stats-cards">
            <div class="stat-card">
                <h4>Total Study Sessions</h4>
                <p class="stat-value">@totalSessions</p>
            </div>
            <div class="stat-card">
                <h4>Total Hours Studied</h4>
                <p class="stat-value">@totalHours.ToString("F1")</p>
            </div>
        </div>
    </Authorized>
    <NotAuthorized>
        <div class="hero-section">
            <div class="hero-content">
                <h1 class="hero-title">📚 Study Tracker</h1>
                <p class="hero-tagline">Track your study progress and achieve your goals</p>
                
                @if (userCount != null)
                {
                    <p class="user-count">
                        @if (userCount.CanRegister)
                        {
                            <span class="badge bg-success">Join @userCount.Count/@userCount.MaxUsers students using Study Tracker!</span>
                        }
                        else
                        {
                            <span class="badge bg-warning text-dark">@userCount.Count/@userCount.MaxUsers spots filled - App at capacity</span>
                        }
                    </p>
                }
                
                <div class="feature-cards">
                    <div class="feature-card">
                        <div class="feature-icon">🎯</div>
                        <h3>Set Goals</h3>
                        <p>Create study goals for subjects and certifications</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">📝</div>
                        <h3>Log Sessions</h3>
                        <p>Track your study hours and progress</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">📊</div>
                        <h3>Monitor Progress</h3>
                        <p>View your achievements and stay motivated</p>
                    </div>
                </div>

                <div class="cta-section">
                    @if (userCount?.CanRegister == true)
                    {
                        <a href="/.auth/login/github" class="btn btn-primary btn-lg">
                            <i class="bi bi-github"></i> Login with GitHub
                        </a>
                    }
                    else if (userCount?.CanRegister == false)
                    {
                        <button class="btn btn-secondary btn-lg" disabled>
                            <i class="bi bi-lock"></i> App at Capacity
                        </button>
                        <p class="text-muted mt-2">We're currently at our 50-user limit. Please check back later!</p>
                    }
                    else
                    {
                        <a href="/.auth/login/github" class="btn btn-primary btn-lg">
                            <i class="bi bi-github"></i> Login with GitHub
                        </a>
                    }
                    <p class="tech-badge">Built with Blazor WebAssembly 🚀</p>
                </div>
            </div>
        </div>
    </NotAuthorized>
</AuthorizeView>

@code {
    private string userName = "User";
    private int totalSessions = 0;
    private double totalHours = 0;
    private UserCountResponse? userCount = null;

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        if (user.Identity?.IsAuthenticated ?? false)
        {
            userName = user.Identity.Name ?? "User";
            
            var sessions = await SessionService.GetUserSessionsAsync();
            totalSessions = sessions.Count;
            totalHours = sessions.Sum(s => s.Hours);
        }
        else
        {
            // Load user count for landing page
            userCount = await UserService.GetUserCountAsync();
        }
    }
}
